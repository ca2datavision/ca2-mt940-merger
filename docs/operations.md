# Operations

## Common Commands

| Command | Purpose |
|---------|---------|
| `npm run dev` | Start dev server |
| `npm run build` | Production build |
| `npm run preview` | Preview production build |
| `npm run lint` | Run ESLint |

## Deployment

GitHub Actions workflow (`.github/workflows/docker.yml`) builds and pushes Docker image.
